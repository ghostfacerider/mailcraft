{
  "name": "mailcraft",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "api": "SET NODE_ENV=development & cd backend && npm start",
    "client": "SET NODE_ENV=development & cd client && npm start",
    "dev": "SET NODE_ENV=development & concurrently \"npm run api\" \"npm run client\" --kill-others --kill-others-on-fail"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}