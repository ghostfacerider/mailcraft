{
  "name": "mailcraft",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "api": "NODE_ENV=development cd api && nodemon start",
    "client": "NODE_ENV=development cd client && npm start",
    "dev": "NODE_ENV=development concurrently \"npm run api\" \" npm run client\" --kill-others --kill-others-on-fail "
  },

  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
